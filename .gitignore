# Auth & secrets
auths/
data/
*.key
*.pem
*.env
.env
.env.*

# Binaries (anchored to repo root only — bare "login"/"credit" would also ignore cmd/login/, cmd/credit/)
/login
/credit
/wb2api
/signin_bin

# Config with secrets (use config.example.json)
config.json

# Build artifacts
*.tmp
__pycache__/

# Web 管理台前端产物(构建期生成,go:embed 仅需目录存在)
internal/web/dist/*
!internal/web/dist/.gitkeep
web/node_modules/

# Backups (contains auth tokens)
backups/

# IDE
.idea/
.vscode/
LOOP.md
PLAN.md
SPEC.md
docs/

# 设计/计划文档（但 README.md 必须进版本库）
*.md
!README.md

# Web 管理台:前端依赖与构建产物(dist 由构建期生成,仅提交 .gitkeep 占位)
node_modules/
internal/web/dist/*
!internal/web/dist/.gitkeep